# Task 004 - 南星 - 集成测试与质量验收

## 任务概述
在苏墨（界面+广告移除）和陆沉（后端+Skill脚本）完成开发后，进行功能集成测试和质量验收。

## 依赖关系
⚠️ **前置任务**：
- Task 001（苏墨 - AI界面完善与广告移除）✅完成
- Task 002（陆沉 - Skill脚本开发）✅完成

**等待时间**：预计苏墨+陆沉需要 1-2 天完成，在他们交付后再开始主要测试工作。

## 工作内容

### 阶段一：测试准备（可立即开始）

#### 1. 分析代码，编写详细测试用例
深入分析 `app_v5.js` 和 `index.html`，编写覆盖以下内容的测试用例：

**AI 生成功能测试用例**：
- 正常生成：所有参数填写完整 → 点击生成 → 等待结果
- 缺少 API Key：未配置 Key → 点击生成 → 预期提示配置
- 无效 API Key：填写错误 Key → 测试连接 → 预期报错
- Provider 切换：OpenAI → Gemini → 验证模型列表更新
- 本地模型：配置 Ollama 地址 → 获取模型 → 验证列表
- 图片选择：选择文件夹 → 验证图片预览
- 复制到编辑器：生成完成 → 复制到编辑器 → 验证内容

**广告移除验收**：
- 顶部广告：打开页面 → 检查顶部导航 → 确认无广告链接
- 浮动广告：等待 5 秒 → 检查右下角 → 确认无浮动广告

#### 2. 浏览器兼容性测试规划
- Chrome 90+
- Edge 90+
- Firefox 88+
- Safari 14+（Mac）

### 阶段二：功能测试（苏墨/陆沉完成后执行）

#### 测试执行清单
使用 F12 开发者工具 Console 检查 JS 错误，逐项验证：

**广告移除验证**：
- [ ] 顶部 header 无 `BookAI`、`AI产品购买` 等广告链接
- [ ] 右下角无浮动广告（`.floating-ad` 元素不存在）
- [ ] JS Console 无 `floatingAd` 相关报错

**AI 配置界面验证**：
- [ ] 切换到「设置」标签 → 可见 Provider 下拉框
- [ ] 选择 OpenAI → 可见 API Key 输入框和模型下拉框
- [ ] 选择 Gemini → 默认模型变为 `gemini-1.5-pro`
- [ ] 选择 Claude → 默认模型变为 `claude-sonnet-4-20250514`
- [ ] 选择本地模型 → 可见 API 地址输入框
- [ ] 填写配置 → 点击「测试连接」→ 验证连接成功/失败提示
- [ ] 填写配置 → 点击「保存配置」→ 刷新页面 → 配置保留

**AI 生成界面验证**：
- [ ] 切换到「AI 生成」标签 → 可见参数配置表单
- [ ] 选择文案类型（产品介绍）→ 下拉框选项正确
- [ ] 选择文案风格（专业严谨）→ 下拉框选项正确
- [ ] 填写字数范围（500-800）→ 数字输入正常
- [ ] 选择图片文件夹 → 图片预览网格显示
- [ ] 填写自定义要求 → textarea 可输入
- [ ] **关键路径**：所有参数填写 → 点击「开始生成」→ 验证 API 被调用 → 验证结果渲染

**Skill 脚本验证**（需要在命令行执行）：
```bash
cd D:\Dev\Project\huasheng_editor
python skills/huasheng-ai-article/scripts/generate_article.py ^
  --type "产品介绍" ^
  --style "专业严谨" ^
  --min-words 500 ^
  --max-words 800 ^
  --output "data/generated"
```
- [ ] 脚本执行不报错
- [ ] `data/generated/` 目录生成 .md 文件
- [ ] 生成的文件包含标题、正文

### 阶段三：回归测试
- 原有 Markdown 编辑功能不受影响（编辑 → 预览 → 复制到公众号）
- 样式切换功能正常
- 图片粘贴/拖拽功能正常

## 交付物
1. 完整的测试用例文档（可更新 `docs/test-cases.md`，与温瑜协作）
2. Bug 报告（格式：`[Bug ID] 描述 | 严重程度 | 重现步骤`）
3. 测试通过报告（最终签字验收）

## 严重程度定义
- **P0（阻断）**：功能完全不可用，必须立即修复
- **P1（严重）**：功能有缺陷但部分可用，24小时内修复
- **P2（一般）**：样式/体验问题，可接受在下一版本修复

## 注意事项
- 所有测试在本地 `python -m http.server 8888` 环境下进行
- 测试前清除浏览器缓存，避免旧 JS 残留
- 重点关注「生成」按钮的完整流程（最核心功能）
- 如发现 JS 错误，先检查是「新增 AI 代码引入」还是「原有代码问题」
